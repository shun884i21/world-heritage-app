# 世界遺産ずかん — 引き継ぎ書

> 新しいチャットはこの1ファイルを読めば作業を継続できます。最終更新: 2026-07-06（592件時点・国別0件を解消・戻るボタン対応済み）

---

## 1. これは何か

世界遺産（全件）を**検索・閲覧して、写真・AI解説を見て、制覇状況を記録できる**個人向けPWA。
ユーザー（俊介さん）の他アプリと同様、バニラJS + GitHub Pages 公開の無料・オフライン対応アプリ。

- **公開URL（ライブ）**: https://shun884i21.github.io/world-heritage-app/
- **GitHubリポジトリ**: https://github.com/shun884i21/world-heritage-app （public）
- **ローカルフォルダ**: `C:\Users\shuns\OneDrive\デスクトップ\claude code\world-heritage-app\`
  - ※2026-06-29 に `デスクトップ直下` から `claude code\` 配下へ移動済み。移動時は [launch.json](#7-プレビューの起動) とメモリのパスも直すこと。
- **プレビュー起動名**: `world-heritage`（ポート4200）。`.claude/launch.json`（デスクトップ直下）に登録済み。

---

## 2. 現在のステータス（完成済み）

- ✅ データ：Wikidataから**全1,171件**取得・整形（`data/sites.json`）
- ✅ アプリ本体：**3タブPWA**（① さがす ② 統計・うんちく ③ コレクション）＋詳細モーダル（写真・地図・Wikipediaリンク）
- ✅ PWA：manifest・Service Worker・アイコン生成済み（ホーム追加・オフライン可）
- ✅ AI解説：**592件完成**（`data/descriptions.json`）= 日本24件 + 世界の著名568件（2026-06-30〜07-06に著名540件を追加。国別0件は07-04に完全解消済み。直近50件は保有数上位の大国の著名サイト＝イタリア12（最後の晩餐/カステル・デル・モンテ/サン・ジミニャーノ/神殿の谷/エオリア諸島等）、フランス9（アルル/ミディ運河/ボルドー/シャンパーニュ/ランス大聖堂等）、中国10（周口店/三孔/武夷山/パンダ保護区/南方カルスト/新疆天山等）、ドイツ9（フェルクリンゲン/博物館島/ツォルフェアアイン/ライン渓谷/ノイシュヴァンシュタイン等）、スペイン10（エル・エスコリアル/ブルゴス大聖堂/ヘラクレスの塔/ザフラー宮殿等）を追加）
- ✅ 統計・うんちくタブ強化（2026-07-03）：今日の世界遺産（日替わり・日付シード）、分類/地域のSVGドーナツグラフ、年代別登録数チャート、記録コーナー（最北/最南/最多国共有など。データから自動算出）、国別ランキングタップ→検索ジャンプ（`gotoCountry`）、うんちく12本＋日替わり
- ✅ コレクションタブ強化（2026-07-03）：称号レベル10段階（`RANKS`）、実績バッジ12種（進捗バーつき、`ACHIEVEMENTS`）、訪れた国/地域ミニ統計、分類別制覇率
- ✅ さがすタブUI刷新（2026-07-03）：カード=画像上グラデ+浮かせバッジ+出現アニメ、丸型検索バー（アイコン内蔵）、チップ横スクロール1行化、解説あり=金ドット表示
- ✅ スマホ戻るボタン対応（2026-07-03）：History APIでタブ切替と詳細モーダルを履歴管理（`setupHistory`/`navTab`/`pushUIState`）。戻る＝モーダル閉→前のタブ→アプリ終了。モーダルの×も`history.back()`経由。SWは`wh-v7`
- ✅ うんちく自動増加（2026-07-06）：`data/trivia.json`（全48本）に移行。各本に `since`（解禁日 YYYY-MM-DD）を持たせ、`app.js` の init で「今日以前のものだけ」を採用＝**毎週1本ずつ勝手に増える**（サーバー/API/定期タスク不要）。今日時点20本表示、残28本を毎週解禁（〜2027-01-19）。オフライン時は `TRIVIA_FALLBACK`（内蔵12本）にフォールバック。**うんちくを増やしたい時は trivia.json に `{h,t,since}` を足すだけ**（sinceを未来日にすればその日に解禁、省略すれば即時）。SWは`wh-v8`
- ✅ セクション切替式に再構成（2026-07-03）：統計=きょう/グラフ/ランキング/記録/うんちくの5切替、コレクション=制覇状況/実績/閲覧済み/行きたいの4切替。サブナビ（`.subnav`）はヘッダー直下にsticky（ヘッダー高は`--header-h`にJSで反映）。うんちくは横スワイプカルーセル（`triviaCarouselHTML`/`bindTriviaCarousel`、←→/🎲/カウンター、今日の分が先頭）。**うんちくを増やすときは `TRIVIA` 配列に足すだけ**（カルーセルなので何話でも縦に伸びない）。SWは`wh-v6`
- ✅ GitHub Pages 公開済み

---

## 3. 一番大事な「次の仕事」＝ AI解説の続き

残り **約579件**（1171件中592件完成）の解説を継続する。方針：**著名どころから順に、5〜13件程度の小バッチでマージ＆プッシュ**しながら積み上げる（1ターンで50件＝4〜8バッチが目安）。ユーザー指示（「ヨーロッパを50件」「次の50件」等）が来たらその範囲を優先。

**次にやるとしたら**: 国別0件は解消済み。保有数上位の大国も07-06に一巡目を追加したが、まだ多くの残がある（2026-07-06時点でイタリア残約26、フランス残約27、中国残約24、ドイツ残約24、スペイン残約21、インド残約25、イギリス残約25、ロシア残約22、メキシコ残約20など）。次の候補は、①これら大国の未執筆分を著名順に継続、②イラン（残約22）・ブラジル（残約15）・トルコ（残約14）・カナダ（残約12）などカバー率が半分以下の中堅国、③国別カバー状況は毎回 `node -e` でsites.jsonとdescriptions.jsonを突き合わせて確認するとよい（引き継ぎ書内の再現手順1を参照）。⚠️ バッチJSONを書いたら件数を必ず数えること（今回50件のつもりが48件で、2件追加して調整した）。

### 絶対のルール（ユーザーとの約束）

> **記憶で書かない。出典（Wikipedia本文）を取得して実際に読み、その記述だけで書く。** 記事に無い年号・人物は書かない。断定できない箇所は「〜とされる」等にトーンダウンする。各解説に出典URL＋取得日を必ず残す。

ハルシネーション防止が最優先。有名サイトはこの方式で十分高品質に書ける。

### 再現手順（このとおりやれば続けられる）

すべて `world-heritage-app\` をカレントに、`node`（v22）で実行。

1. **対象IDを特定**（`data/sites.json` を名前/国で検索）。`scripts/` に検索用の使い捨てスクリプトを作ってもよい。例: 国名 `s.countries.includes("フランス")`、名前 `s.name.includes("...")`。
   - ⚠️ 著名サイトでも `name` が英語のまま（Wikidataに日本語名なし）や `ja_wiki` が無いものがある。その場合は後述の対処。

2. **出典本文を取得**: `node scripts/fetch_wiki.mjs <id1> <id2> ...`
   - 各IDの `ja_wiki`（無ければ `en_wiki`）からMediaWiki APIで本文プレーンテキストを取得し `data/wiki_cache/<id>.json` に保存（冒頭3000字に制限）。
   - **Wikipediaリンクが無いID**は、タイトル直指定で取る必要がある（過去に佐渡=1698、アンコール=668、万里の長城=438、ガラパゴス=1、ギザ=86、ヴェルサイユ=83、クレムリン=545でやった）。汎用スクリプト **`node scripts/fetch_wiki_title.mjs <id> <ja|en> <タイトル>`** で `data/wiki_cache/<id>.json` を生成できる（フォーマットは他のキャッシュと同じ）。

3. **精読して執筆**: `data/wiki_cache/<id>.json` を Read で読み、各遺産につき以下3節を**本文の事実だけ**で書く。
   - `background`（歴史的背景, 4〜6文）/ `people`（関わった人々）/ `highlights`（見どころ）
   - さらに `source_url`（cacheのと同じ）, `source_label`（例「Wikipedia「姫路城」」）, `retrieved`（cacheのと同じ日付）。
   - 文体は既存の解説群に合わせる（です・ます調、観光客が読んで面白い温度感、登録基準は「登録基準(9)」のように軽く触れる程度）。
   - **自然遺産で「関わった人物」が薄い場合**は、保全活動・発見者・命名の由来などを `people` に書く（無理に人物を捏造しない）。

4. **バッチJSONにまとめてマージ**: 数件〜10件を1つの `batch.json`（`{ "<id>": {background,people,highlights,source_url,source_label,retrieved}, ... }`）として scratchpad等に書き、
   `node scripts/merge_descriptions.mjs <batch.jsonのパス>` で `data/descriptions.json` に統合（id昇順で保存、既存は上書き）。

5. **公開反映**: `git add -A && git commit && git push`（下記 [8. デプロイ](#8-デプロイ更新の反映)）。数分でライブ反映。

### 日本語名が無い著名サイトの扱い

`scripts/build_sites.mjs` の `NAME_OVERRIDE`（id→日本語名）に追記して `node scripts/build_sites.mjs` で `sites.json` を再生成する。現在: 438万里の長城 / 1ガラパゴス諸島 / 86ギザ / 83ヴェルサイユ / 162アミアン大聖堂（Wikidata名"ノートルダム大聖堂"は誤解を招くため上書き）/ 29クラクフ歴史地区 / 80モン・サン＝ミシェルとその湾 / 95ドゥブロヴニクの旧市街 / 97スプリトの史跡群とディオクレティアヌス宮殿 / 64ティカル国立公園 / 728エディンバラの旧市街と新市街 / 383セビリアの大聖堂・アルカサル・インディアス古文書館 / 541ヴィリニュス旧市街を登録済み。

---

## 4. データ構造

### `data/sites.json`（1171件の配列）
各要素: `id`(UNESCO参照番号), `name`(日本語、無ければ英語/上書き), `name_en`, `type`(cultural/natural/mixed/unknown), `criteria`(例"(i),(iv)"), `year`, `countries`[], `danger`(bool), `lat`, `lon`, `image`(Wikimedia Commons FilePath URL), `ja_wiki`, `en_wiki`, `qid`

### `data/descriptions.json`（id→解説、現在392件）
`{ "661": { background, people, highlights, source_url, source_label, retrieved } }`
アプリは起動時に読み込み、詳細モーダルで該当IDがあれば表示、無ければ「準備中」表示。

---

## 5. データ生成の元（再ビルド可能）

- `data/wikidata_raw.json`（5.5MB）= Wikidata SPARQL結果の生データ。`build_sites.mjs` の入力。
- `data/danger_raw.json` / `data/danger_refs.json` = 危機遺産リスト（Wikipedia「List of World Heritage in Danger」のCurrent listから抽出した参照番号）。
- `data/wiki_cache/<id>.json` = 各遺産のWikipedia本文キャッシュ（解説執筆の根拠）。
- `scripts/build_sites.mjs` … 生データ→sites.json（名寄せ・分類導出・危機遺産付与・名前上書き）
- `scripts/extract_danger.mjs` … 危機遺産参照番号の抽出
- `scripts/fetch_wiki.mjs` … Wikipedia本文取得
- `scripts/merge_descriptions.mjs` … バッチを descriptions.json に統合
- `scripts/make_icons.mjs` … アイコンPNG生成

---

## 6. アプリのファイル

- `index.html` … 3タブ＋詳細モーダルのDOM
- `css/style.css` … スタイル（緑×金の図鑑風テーマ）
- `js/app.js` … 全ロジック（フィルタ/検索/詳細/統計/コレクション/地図/SW登録）
  - 検索は フリーワード＋分類＋国/地域＋危機遺産＋並び替えを **AND重ねがけ**。地域マッピングは `REGION_MAP`。
  - コレクションは localStorage キー `wh_collection_v1`。
  - 地図は詳細を開いた時にLeaflet（CDN）を遅延ロード。
- `sw.js` … Service Worker（**cache `wh-v4`**。同一オリジンを成功時のみキャッシュ。**配信ファイルを更新したら必ず `CACHE` のバージョンを上げる**＝古いキャッシュ配信を防ぐ。install時は `fetch(u,{cache:"reload"})` でHTTPキャッシュをバイパスし必ず最新を取得。検証時はSW登録解除＋caches.delete＋**リロード2回**（1回目でSW再インストール、2回目でSW配信）が確実）
- `manifest.webmanifest`, `icons/icon-192.png`, `icons/icon-512.png`

> パスはすべて相対。GitHub Pagesのサブパス（/world-heritage-app/）でそのまま動く。

---

## 7. プレビューの起動

`preview_start`（name=`world-heritage`, port 4200）。`.claude/launch.json` の該当エントリは
`--directory "claude code/world-heritage-app"`（フォルダ移動後の値）。
※フォルダをまた移動したら、この launch.json と本書・メモリのパスを直すこと。
検証で古いサーバーが `reused` された場合は `preview_stop` してから `preview_start` で起動し直す。

---

## 8. デプロイ（更新の反映）

新パスから:
```
git -C "C:\Users\shuns\OneDrive\デスクトップ\claude code\world-heritage-app" add -A
git -C "...同上..." commit -m "メッセージ" 
git -C "...同上..." push
```
gh認証は `shun884i21`（keyring, repoスコープあり）。push後 数分でライブ反映。
コミット末尾には `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` を付ける。

---

## 9. 既知の残課題

- AI解説：残り約579件（指示が来たら継続。50件区切りで依頼されることが多い）
- `type=unknown` 8件（Wikidataに登録基準が無い）。必要なら手で分類補完。
- 日本語名なし 約290件 → 英語名で代替表示中（重要サイトは NAME_OVERRIDE で対応）
- 登録年が怪しいものは `build_sites.mjs` の `YEAR_OVERRIDE` で上書き（現在: 156セレンゲティ 1960→1981）
- 危機遺産：現役約56件中45件を反映（取り込めた1171件と一致した分のみ）
- 未取得の公式物件 約50件（枝番ID形式等でSPARQLから漏れた分）
- ~~統計ページの円グラフ等の作り込みは未着手~~ → 2026-07-03 に完了（ドーナツグラフ・年代チャート・記録コーナー・称号/実績）

---

## 10. ユーザーの進め方の希望（重要）

- **重要な仕様・要件の確認以外は、常に許可。止めずに自走してよい。**（パーミッションも基本許可）
- アプリ移動時は launch.json / 定期タスク / メモリ / 本書のパスを追従修正する。
- 解説は出典準拠・事実ベースを最優先（[3章のルール](#絶対のルールユーザーとの約束)）。
